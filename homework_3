#1. Создайте два списка с вопросами и ответами.
 # questions = ("Му name _ Vova", "I _ а coder", "I live _ Moscow"] answers = ["is", "am", "in"] 
#2. Сперва программа здоровается и предлагает начать: Привет! Предлагаю проверить свои знания английского! Наберите "ready", чтобы начать! 
 # Если пользователь набрал "ready" - программа начинает задавать вопросы.
  
  # Если нет - программа завершается с сообщением: 
  # Кажется, вы не хотите играть. Очень жаль . 
  
  # Когда первый вопрос задан, приложение ждет ввод пользователя. 
  # Если ответ правильный, приложение говорит: ответ верный,
  # Если нет, говорит: Неправильно. Правильный ответ: 

#3. Затем приложение задает следующий вопрос. 
 # После ответа на все вопросы приложение говорит: Вот и все! вы ответили на _ вопросов из _ верно, это __ процентов.
#4.* Если ответ неверный, приложение говорит: Осталось попыток: 2, попробуйте еще раз! 
 # Если ответ опять неверный, говорит: Осталось попыток: 1, попробуйте еще раз 1 
 # Если еще раз вводится неправильный ответ, то приложение говорит: 
 # Увы, но нет. Верный ответ: 
  
 # За каждый ответ начисляются баллы. 
 # Если с первой попытки введен правильный ответ- 3 балла, со второй - 2, с третьей - 1. После ответа на все вопросы приложение говорит: 
 # вот и все! вы ответили на _ вопросов из _ верно, вы набрали _ баллов.
 
 # Создаем 2 списка, с вопросами и ответами
questions = ['My name _ Vova', 'I _ a coder', 'I live _ Moscow']
answers = ['is', 'am', 'in']

# Приветствуем пользователя и предлагаем ввести команду "ready"
print('Привет! Предлагаю проверить свои знания английского! Наберите "ready", чтобы начать!')
user_command = input('Введите команду: ')

# Определяем 2 переменные, одна для подсчета индексов, другая для подсчета правильных ответов
count_answers = -1           # Счетчик для прибавления индексов
count_write = 0              # Счетчик правильных ответов
go = 3                       # Количество попыток
count_go = 0                 # Счетчик количества баллов

# Вводим условие при котором запускается цикл вопросов
if user_command == 'ready':

    # Создаем цикл вопросов в котором при правильном ответе ведется их подсчет.
    for user_question in questions:
        print(user_question)
        count_answers += 1
        go = 3

        # Создаем новый цикл для подсчета попыток
        while True:
            user_answer = input('Введите ответ: ')

            if user_answer == answers[count_answers]:
                print('Ответ верный')
                count_write += 1
                count_go += go
                break

            elif user_answer != answers[count_answers]:
                go -= 1

                if go == 2:
                    print(f'Осталось попыток: {go}, попробуйте еще раз!')

                elif go == 1:
                    print(f'Осталось попыток: {go}, попробуйте еще раз!')

                else:
                    print(f'Увы, но нет. Верный ответ: {answers[count_answers]}')
                    break


    # Высчитываем количество вопросов в списке и подсчитываем процент правильных ответов
    number_questions = len(questions)
    percent = round(count_write / number_questions * 100, 2)

    # Подводим итоги работы программы
    print(f'Вот и все! \n'
      f'Вы ответили на {count_write} вопросов из {number_questions} верно, \n'
      f'это {percent} процентов. \n'
      f'Вы набрали {count_go} баллов.'
    )

else:
    print('Кажется, вы не хотите играть. Очень жаль.')
